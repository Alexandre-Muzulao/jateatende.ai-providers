export enum UserRole {
  CUSTOMER = "CUSTOMER",
  PROVIDER = "PROVIDER",
  ADMIN = "ADMIN",
}

export enum UserAccountStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  POST_REGISTRATION = "POST_REGISTRATION",
}
